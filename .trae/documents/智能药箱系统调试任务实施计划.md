# 智能药箱系统调试任务实施计划

> **目标：** 修复代码审查中发现的问题，完善系统功能，提高代码质量和稳定性

**架构：** 基于 STM32G431RBT6 的嵌入式系统，使用 FreeRTOS 进行任务调度，通过 I2C 接口连接传感器，通过 USART 连接 WiFi 模块

**技术栈：** STM32 HAL 库、FreeRTOS、CMSIS-RTOS V2、I2C、USART、GPIO

---

## 任务 1: 修复 main.c 初始化代码

**问题：** main.c 缺少项目特定的初始化代码，没有添加传感器和通信模块的头文件，也没有实现 USART3 接收中断的启动和应用初始化。

**文件：**
- 修改：`Core/Src/main.c`

**步骤 1: 添加头文件引用**

在 `/* USER CODE BEGIN Includes */` 区域添加：
```c
/* USER CODE BEGIN Includes */
#include "mpu6050.h"
#include "aht20.h"
#include "bmp280.h"
#include "sensor_manager.h"
#include "esp8266.h"
#include "app_tasks.h"
/* USER CODE END Includes */
```

**步骤 2: 添加初始化代码**

在 `/* USER CODE BEGIN 2 */` 区域添加：
```c
/* USER CODE BEGIN 2 */
// 启动USART3接收中断
static uint8_t esp_rx_byte;
HAL_UART_Receive_IT(&huart3, &esp_rx_byte, 1);

// 初始化应用
App_Init();
/* USER CODE END 2 */
```

**步骤 3: 修改 FreeRTOS 初始化**

将 `MX_FREERTOS_Init()` 调用改为：
```c
// 注释掉自动生成的初始化
// MX_FREERTOS_Init();

// 启动自定义任务
App_StartTasks();
```

**步骤 4: 添加中断回调函数**

在 `/* USER CODE BEGIN 4 */` 区域添加：
```c
/* USER CODE BEGIN 4 */
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    static uint8_t esp_rx_byte;
    
    if (huart->Instance == USART3)
    {
        ESP8266_UART_RxCallback(esp_rx_byte);
        HAL_UART_Receive_IT(&huart3, &esp_rx_byte, 1);
    }
}
/* USER CODE END 4 */
```

---

## 任务 2: 添加传感器错误处理和重试机制

**问题：** 传感器数据读取没有完整的错误处理和重试机制。

**文件：**
- 修改：`MDK-ARM/code/sensor_manager.c`

**步骤 1: 修改 SensorManager_ReadAll 函数**

添加错误计数和重试逻辑：
```c
uint8_t SensorManager_ReadAll(void)
{
    uint8_t result = 0;
    uint8_t retry_count = 0;
    uint8_t max_retries = 3;
    
    /* 读取MPU6050 */
    retry_count = 0;
    while (retry_count < max_retries) {
        if (MPU6050_ReadData(&mpu_data) == 0) {
            MPU6050_CalculateAngles(&mpu_data);
            // ... 数据处理代码 ...
            break;
        } else {
            retry_count++;
            HAL_Delay(10);
        }
    }
    if (retry_count >= max_retries) {
        result |= 0x01;
        printf("[Sensor] MPU6050 read failed after %d retries\r\n", max_retries);
    }
    
    /* 读取AHT20 */
    retry_count = 0;
    while (retry_count < max_retries) {
        if (AHT20_ReadDataBlocking(&aht_data, 200) == 0) {
            // ... 数据处理代码 ...
            break;
        } else {
            retry_count++;
            HAL_Delay(10);
        }
    }
    if (retry_count >= max_retries) {
        result |= 0x02;
        printf("[Sensor] AHT20 read failed after %d retries\r\n", max_retries);
    }
    
    /* 读取BMP280 */
    retry_count = 0;
    while (retry_count < max_retries) {
        if (BMP280_ReadData(&bmp_data) == 0) {
            // ... 数据处理代码 ...
            break;
        } else {
            retry_count++;
            HAL_Delay(10);
        }
    }
    if (retry_count >= max_retries) {
        result |= 0x04;
        printf("[Sensor] BMP280 read failed after %d retries\r\n", max_retries);
    }
    
    /* 检测药箱状态 */
    box_data.state = SensorManager_DetectState();
    box_data.timestamp = HAL_GetTick();
    box_data.is_valid = (result == 0) ? 1 : 0;
    
    return result;
}
```

---

## 任务 3: 优化 MQTT 通信错误处理

**问题：** MQTT 连接和数据发布没有完整的错误处理。

**文件：**
- 修改：`MDK-ARM/code/app_tasks.c`

**步骤 1: 添加 MQTT 重连机制**

修改 MQTTTask 函数中的状态机：
```c
void MQTTTask(void *argument)
{
    uint8_t wifi_connected = 0;
    uint8_t mqtt_connected = 0;
    uint8_t retry_count = 0;
    uint8_t max_retries = 5;
    char json_buffer[512];
    uint32_t last_publish_tick = 0;
    
    printf("[MQTTTask] Started\r\n");
    
    /* 等待系统就绪 */
    osDelay(1000);
    
    for (;;) {
        /* 状态机处理 */
        switch (ESP8266_GetState()) {
            case ESP8266_STATE_RESET:
            case ESP8266_STATE_INIT:
                /* 初始化WiFi */
                printf("[MQTT] Initializing WiFi...\r\n");
                if (ESP8266_WiFi_Init() == 0) {
                    printf("[MQTT] WiFi AT OK\r\n");
                } else {
                    printf("[MQTT] WiFi Init failed, retry...\r\n");
                    ESP8266_Reset();
                    osDelay(2000);
                }
                osDelay(500);
                break;
                
            case ESP8266_STATE_WIFI_CONNECTING:
                /* 等待连接完成 */
                osDelay(500);
                break;
                
            case ESP8266_STATE_WIFI_CONNECTED:
                /* WiFi已连接，连接MQTT */
                if (!mqtt_connected) {
                    printf("[MQTT] Connecting to broker...\r\n");
                    
                    if (ESP8266_MQTT_Init(MQTT_BROKER_IP, MQTT_BROKER_PORT) == 0) {
                        if (ESP8266_MQTT_ConnectToBroker(MQTT_BROKER_IP, MQTT_BROKER_PORT) == 0) {
                            printf("[MQTT] Connected to %s:%d\r\n", MQTT_BROKER_IP, MQTT_BROKER_PORT);
                            
                            /* 订阅控制主题 */
                            ESP8266_MQTT_Subscribe(MQTT_TOPIC_CONTROL, 0);
                            
                            /* 发布上线消息 */
                            ESP8266_MQTT_Publish(MQTT_TOPIC_STATUS, "online", 0, 1);
                            
                            mqtt_connected = 1;
                            retry_count = 0;
                        } else {
                            retry_count++;
                            printf("[MQTT] Connect failed, retry %d/%d\r\n", retry_count, max_retries);
                        }
                    } else {
                        retry_count++;
                        printf("[MQTT] Init failed, retry %d/%d\r\n", retry_count, max_retries);
                    }
                    
                    if (!mqtt_connected) {
                        if (retry_count >= max_retries) {
                            printf("[MQTT] Connection failed after %d retries, reset...\r\n", max_retries);
                            ESP8266_Reset();
                            retry_count = 0;
                            wifi_connected = 0;
                        }
                        osDelay(3000);
                    }
                }
                break;
                
            case ESP8266_STATE_MQTT_CONNECTED:
                /* MQTT已连接，等待发布信号 */
                if (osSemaphoreAcquire(mqttPublishSem, 100) == osOK) {
                    /* 生成JSON数据 */
                    SensorManager_CreateJSON(json_buffer, sizeof(json_buffer));
                    
                    /* 发布数据 */
                    uint8_t pub_retry = 0;
                    while (pub_retry < 3) {
                        if (ESP8266_MQTT_Publish(MQTT_TOPIC_DATA, json_buffer, 0, 0) == 0) {
                            printf("[MQTT] Published: %s\r\n", json_buffer);
                            last_publish_tick = HAL_GetTick();
                            break;
                        } else {
                            pub_retry++;
                            printf("[MQTT] Publish failed, retry %d/3\r\n", pub_retry);
                            osDelay(500);
                        }
                    }
                    
                    if (pub_retry >= 3) {
                        printf("[MQTT] Publish failed after 3 retries\r\n");
                        mqtt_connected = 0;
                    }
                }
                
                /* 检查是否需要强制发布（每30秒） */
                if ((HAL_GetTick() - last_publish_tick) > 30000) {
                    osSemaphoreRelease(mqttPublishSem);
                }
                
                /* 处理接收 */
                ESP8266_ProcessRxData();
                break;
                
            case ESP8266_STATE_ERROR:
                printf("[MQTT] Error state, resetting...\r\n");
                ESP8266_Reset();
                wifi_connected = 0;
                mqtt_connected = 0;
                retry_count = 0;
                osDelay(2000);
                break;
                
            default:
                break;
        }
        
        /* WiFi连接管理 */
        if (!wifi_connected && ESP8266_GetState() < ESP8266_STATE_WIFI_CONNECTING) {
            printf("[MQTT] Connecting to WiFi: %s\r\n", WIFI_SSID);
            if (ESP8266_WiFi_Connect(WIFI_SSID, WIFI_PASSWORD) == 0) {
                printf("[MQTT] WiFi Connected\r\n");
                wifi_connected = 1;
                retry_count = 0;
            } else {
                retry_count++;
                printf("[MQTT] WiFi Connect failed, retry %d/%d\r\n", retry_count, max_retries);
                if (retry_count >= max_retries) {
                    printf("[MQTT] WiFi connection failed after %d retries, reset ESP8266...\r\n", max_retries);
                    ESP8266_Reset();
                    retry_count = 0;
                }
                osDelay(3000);
            }
        }
        
        /* 如果WiFi断开，重置状态 */
        if (wifi_connected && ESP8266_WiFi_IsConnected() == 0) {
            if (ESP8266_GetState() == ESP8266_STATE_WIFI_CONNECTED ||
                ESP8266_GetState() == ESP8266_STATE_MQTT_CONNECTED) {
                printf("[MQTT] WiFi disconnected\r\n");
                wifi_connected = 0;
                mqtt_connected = 0;
                retry_count = 0;
            }
        }
        
        osDelay(100);
    }
}
```

---

## 任务 4: 优化振动检测算法

**问题：** 振动检测算法使用的是简单的加速度合成减去重力，可能不够准确。

**文件：**
- 修改：`MDK-ARM/code/sensor_manager.c`

**步骤 1: 添加历史数据缓存**

在 sensor_manager.c 中添加：
```c
/* 振动检测历史数据 */
#define VIBRATION_HISTORY_SIZE  10
static float accel_history[VIBRATION_HISTORY_SIZE][3];
static uint8_t history_index = 0;
static uint8_t history_count = 0;
```

**步骤 2: 修改振动检测函数**

```c
/**
  * @brief  更新加速度历史数据
  * @param  accel_x, accel_y, accel_z: 当前加速度值
  */
static void UpdateAccelHistory(float accel_x, float accel_y, float accel_z)
{
    accel_history[history_index][0] = accel_x;
    accel_history[history_index][1] = accel_y;
    accel_history[history_index][2] = accel_z;
    
    history_index = (history_index + 1) % VIBRATION_HISTORY_SIZE;
    if (history_count < VIBRATION_HISTORY_SIZE) {
        history_count++;
    }
}

/**
  * @brief  计算振动强度
  * @retval 振动强度值
  */
static float CalculateVibration(void)
{
    if (history_count < 2) {
        return 0.0f;
    }
    
    float sum_diff = 0.0f;
    uint8_t count = 0;
    
    for (uint8_t i = 1; i < history_count; i++) {
        uint8_t curr_idx = (history_index + VIBRATION_HISTORY_SIZE - i) % VIBRATION_HISTORY_SIZE;
        uint8_t prev_idx = (history_index + VIBRATION_HISTORY_SIZE - i - 1) % VIBRATION_HISTORY_SIZE;
        
        float diff_x = accel_history[curr_idx][0] - accel_history[prev_idx][0];
        float diff_y = accel_history[curr_idx][1] - accel_history[prev_idx][1];
        float diff_z = accel_history[curr_idx][2] - accel_history[prev_idx][2];
        
        sum_diff += sqrtf(diff_x * diff_x + diff_y * diff_y + diff_z * diff_z);
        count++;
    }
    
    return (count > 0) ? (sum_diff / count) : 0.0f;
}
```

**步骤 3: 修改 SensorManager_ReadAll 函数**

在读取 MPU6050 数据后添加：
```c
/* 更新历史数据 */
UpdateAccelHistory(mpu_data.accel_x_g, mpu_data.accel_y_g, mpu_data.accel_z_g);

/* 计算振动强度 */
box_data.motion.vibration = CalculateVibration();
```

---

## 任务 5: 添加调试日志系统

**问题：** 系统缺少统一的调试日志系统。

**文件：**
- 创建：`MDK-ARM/code/debug_log.h`
- 创建：`MDK-ARM/code/debug_log.c`

**步骤 1: 创建 debug_log.h**

```c
/**
  ******************************************************************************
  * @file    debug_log.h
  * @brief   调试日志系统
  ******************************************************************************
  */
#ifndef __DEBUG_LOG_H
#define __DEBUG_LOG_H

#include "main.h"
#include <stdio.h>

/* 日志级别 */
typedef enum {
    LOG_LEVEL_DEBUG = 0,
    LOG_LEVEL_INFO,
    LOG_LEVEL_WARN,
    LOG_LEVEL_ERROR
} LogLevel_t;

/* 日志模块 */
typedef enum {
    LOG_MODULE_SYSTEM = 0,
    LOG_MODULE_SENSOR,
    LOG_MODULE_WIFI,
    LOG_MODULE_MQTT,
    LOG_MODULE_TASK
} LogModule_t;

/* 配置 */
#define CURRENT_LOG_LEVEL   LOG_LEVEL_DEBUG
#define ENABLE_LOG_TIMESTAMP 1
#define ENABLE_LOG_MODULE    1

/* 日志宏 */
#define LOG_DEBUG(module, fmt, ...) DebugLog(LOG_LEVEL_DEBUG, module, fmt, ##__VA_ARGS__)
#define LOG_INFO(module, fmt, ...)  DebugLog(LOG_LEVEL_INFO, module, fmt, ##__VA_ARGS__)
#define LOG_WARN(module, fmt, ...)  DebugLog(LOG_LEVEL_WARN, module, fmt, ##__VA_ARGS__)
#define LOG_ERROR(module, fmt, ...) DebugLog(LOG_LEVEL_ERROR, module, fmt, ##__VA_ARGS__)

/* 函数声明 */
void DebugLog_Init(void);
void DebugLog(LogLevel_t level, LogModule_t module, const char *fmt, ...);
const char* DebugLog_GetLevelString(LogLevel_t level);
const char* DebugLog_GetModuleString(LogModule_t module);

#endif /* __DEBUG_LOG_H */
```

**步骤 2: 创建 debug_log.c**

```c
/**
  ******************************************************************************
  * @file    debug_log.c
  * @brief   调试日志系统实现
  ******************************************************************************
  */
#include "debug_log.h"
#include <stdarg.h>
#include <string.h>

extern UART_HandleTypeDef huart1;

static const char* level_strings[] = {
    "DEBUG", "INFO", "WARN", "ERROR"
};

static const char* module_strings[] = {
    "SYS", "SENSOR", "WIFI", "MQTT", "TASK"
};

void DebugLog_Init(void)
{
    /* 初始化完成 */
}

const char* DebugLog_GetLevelString(LogLevel_t level)
{
    if (level <= LOG_LEVEL_ERROR) {
        return level_strings[level];
    }
    return "UNKNOWN";
}

const char* DebugLog_GetModuleString(LogModule_t module)
{
    if (module <= LOG_MODULE_TASK) {
        return module_strings[module];
    }
    return "UNKNOWN";
}

void DebugLog(LogLevel_t level, LogModule_t module, const char *fmt, ...)
{
    if (level < CURRENT_LOG_LEVEL) {
        return;
    }
    
    char buffer[256];
    uint16_t len = 0;
    
    /* 添加时间戳 */
    #if ENABLE_LOG_TIMESTAMP
    len += snprintf(buffer + len, sizeof(buffer) - len, "[%lu]", HAL_GetTick());
    #endif
    
    /* 添加日志级别 */
    len += snprintf(buffer + len, sizeof(buffer) - len, "[%s]", DebugLog_GetLevelString(level));
    
    /* 添加模块 */
    #if ENABLE_LOG_MODULE
    len += snprintf(buffer + len, sizeof(buffer) - len, "[%s]", DebugLog_GetModuleString(module));
    #endif
    
    /* 添加内容 */
    len += snprintf(buffer + len, sizeof(buffer) - len, " ");
    
    va_list args;
    va_start(args, fmt);
    len += vsnprintf(buffer + len, sizeof(buffer) - len, fmt, args);
    va_end(args);
    
    /* 添加换行 */
    len += snprintf(buffer + len, sizeof(buffer) - len, "\r\n");
    
    /* 输出到串口 */
    HAL_UART_Transmit(&huart1, (uint8_t*)buffer, len, 100);
}
```

---

## 任务 6: 添加系统状态监测

**问题：** 系统缺少状态监测和故障诊断功能。

**文件：**
- 创建：`MDK-ARM/code/system_monitor.h`
- 创建：`MDK-ARM/code/system_monitor.c`

**步骤 1: 创建 system_monitor.h**

```c
/**
  ******************************************************************************
  * @file    system_monitor.h
  * @brief   系统状态监测
  ******************************************************************************
  */
#ifndef __SYSTEM_MONITOR_H
#define __SYSTEM_MONITOR_H

#include "main.h"

/* 系统状态 */
typedef struct {
    uint32_t uptime;            // 运行时间
    uint32_t free_heap;         // 剩余堆内存
    uint32_t cpu_usage;         // CPU使用率
    uint8_t sensor_status;      // 传感器状态
    uint8_t wifi_status;        // WiFi状态
    uint8_t mqtt_status;        // MQTT状态
    uint32_t last_sensor_read;  // 上次传感器读取时间
    uint32_t last_mqtt_publish; // 上次MQTT发布时间
    uint32_t error_count;       // 错误计数
} SystemStatus_t;

/* 函数声明 */
void SystemMonitor_Init(void);
void SystemMonitor_Update(void);
void SystemMonitor_GetStatus(SystemStatus_t *status);
void SystemMonitor_ReportError(const char *error_msg);
void SystemMonitor_PrintStatus(void);

#endif /* __SYSTEM_MONITOR_H */
```

**步骤 2: 创建 system_monitor.c**

```c
/**
  ******************************************************************************
  * @file    system_monitor.c
  * @brief   系统状态监测实现
  ******************************************************************************
  */
#include "system_monitor.h"
#include "sensor_manager.h"
#include "esp8266.h"
#include <stdio.h>

static SystemStatus_t sys_status;
static uint32_t start_tick;

void SystemMonitor_Init(void)
{
    memset(&sys_status, 0, sizeof(sys_status));
    start_tick = HAL_GetTick();
    printf("[Monitor] System monitor initialized\r\n");
}

void SystemMonitor_Update(void)
{
    sys_status.uptime = (HAL_GetTick() - start_tick) / 1000;
    
    /* 获取传感器状态 */
    MedicineBoxData_t data;
    SensorManager_GetData(&data);
    sys_status.sensor_status = data.is_valid;
    
    /* 获取WiFi状态 */
    sys_status.wifi_status = (ESP8266_GetState() >= ESP8266_STATE_WIFI_CONNECTED) ? 1 : 0;
    
    /* 获取MQTT状态 */
    sys_status.mqtt_status = (ESP8266_GetState() == ESP8266_STATE_MQTT_CONNECTED) ? 1 : 0;
}

void SystemMonitor_GetStatus(SystemStatus_t *status)
{
    if (status != NULL) {
        memcpy(status, &sys_status, sizeof(SystemStatus_t));
    }
}

void SystemMonitor_ReportError(const char *error_msg)
{
    sys_status.error_count++;
    printf("[Monitor] Error #%lu: %s\r\n", sys_status.error_count, error_msg);
}

void SystemMonitor_PrintStatus(void)
{
    printf("\r\n========== System Status ==========\r\n");
    printf("Uptime: %lu seconds\r\n", sys_status.uptime);
    printf("Sensor: %s\r\n", sys_status.sensor_status ? "OK" : "FAIL");
    printf("WiFi: %s\r\n", sys_status.wifi_status ? "Connected" : "Disconnected");
    printf("MQTT: %s\r\n", sys_status.mqtt_status ? "Connected" : "Disconnected");
    printf("Errors: %lu\r\n", sys_status.error_count);
    printf("====================================\r\n\r\n");
}
```

---

## 实施建议

1. **按顺序实施：** 建议按照任务 1-6 的顺序依次实施，每个任务完成后进行测试。

2. **版本控制：** 每个任务完成后提交代码，便于回溯和排查问题。

3. **测试策略：**
   - 任务 1：编译代码，检查是否有编译错误。
   - 任务 2：测试传感器数据读取，检查错误处理和重试机制。
   - 任务 3：测试 MQTT 连接和数据发布，检查重连机制。
   - 任务 4：测试振动检测，检查算法准确性。
   - 任务 5：测试日志输出，检查日志格式和内容。
   - 任务 6：测试系统状态监测，检查状态报告功能。

4. **注意事项：**
   - 修改代码前备份原始文件。
   - 每次修改后进行编译和测试。
   - 遇到问题及时记录和排查。
   - 保持代码风格一致性。

---

**计划完成时间：** 预计每个任务需要 30-60 分钟，总计约 3-6 小时。

**预期成果：**
- 修复 main.c 初始化问题
- 提高传感器数据读取的可靠性
- 增强 MQTT 通信的稳定性
- 优化振动检测算法
- 添加统一的调试日志系统
- 实现系统状态监测功能