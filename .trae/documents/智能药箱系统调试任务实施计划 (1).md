## 实施计划概述

根据 `智能药箱系统调试任务实施计划.md` 文档，需要对智能药箱嵌入式项目进行6个任务的修正：

### 任务1: 修复 main.c 初始化代码
**文件**: `Core/Src/main.c`
- 在 `/* USER CODE BEGIN Includes */` 区域添加头文件引用（mpu6050.h, aht20.h, bmp280.h, sensor_manager.h, esp8266.h, app_tasks.h）
- 在 `/* USER CODE BEGIN 2 */` 区域添加 USART3 接收中断启动和 App_Init() 调用
- 修改 MX_FREERTOS_Init() 为 App_StartTasks()
- 在 `/* USER CODE BEGIN 4 */` 区域添加 HAL_UART_RxCpltCallback 中断回调

### 任务2: 添加传感器错误处理和重试机制
**文件**: `MDK-ARM/code/sensor_manager.c`
- 修改 `SensorManager_ReadAll()` 函数
- 为 MPU6050、AHT20、BMP280 分别添加最多3次重试机制
- 添加错误日志输出

### 任务3: 优化 MQTT 通信错误处理
**文件**: `MDK-ARM/code/app_tasks.c`
- 重写 `MQTTTask()` 函数的状态机
- 添加 WiFi 和 MQTT 连接的详细重试机制（最多5次重试）
- 添加数据发布的重试逻辑（最多3次）
- 添加强制发布机制（每30秒）

### 任务4: 优化振动检测算法
**文件**: `MDK-ARM/code/sensor_manager.c`
- 添加振动检测历史数据缓存（10个样本）
- 实现 `UpdateAccelHistory()` 和 `CalculateVibration()` 函数
- 修改振动计算为基于历史数据变化的算法

### 任务5: 添加调试日志系统
**文件**: 
- 创建 `MDK-ARM/code/debug_log.h`
- 创建 `MDK-ARM/code/debug_log.c`
- 实现分级日志（DEBUG/INFO/WARN/ERROR）和模块化日志功能

### 任务6: 添加系统状态监测
**文件**:
- 创建 `MDK-ARM/code/system_monitor.h`
- 创建 `MDK-ARM/code/system_monitor.c`
- 实现系统状态结构体和监测功能

### 实施顺序
按任务1→6顺序实施，每个任务完成后可进行编译验证。

请确认此计划后，我将开始执行具体的代码修改。